# Optional. Only needed if you want the Vite dev server to run on a specific
# port or proxy /api requests somewhere other than http://localhost:5000.
# PORT=5173
# VITE_API_PROXY_TARGET=http://localhost:5000
